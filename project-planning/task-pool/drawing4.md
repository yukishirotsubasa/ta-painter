# drawing4 — 多條線管理與刪除單條線

## 說明

擴充 `DrawingController` 支援管理多條線（陣列），提供選取某條線並刪除的互動（例如點擊線條後出現刪除按鈕，或選取後按 Delete 鍵）。

## 依賴

drawing2

## 驗收方式

1. 可連續畫出多條線，全部正確顯示。
2. 選取其中一條線並刪除，只有該條線消失，其餘線條不受影響。
3. 刪除後的線條資料正確從內部陣列移除（不會殘留在後續的 URL 分享匯出中）。
